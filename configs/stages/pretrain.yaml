train:
  batch_size: 2
  total_steps: 16000
  grad_accum_steps: 32
  early_stopping: 50
  log_every: 10

optim:
  lr: 3e-4
  betas: [0.9, 0.95]
  weight_decay: 0.1

scheduler:
  eta_min_ratio: 0.1

data:
  train_path: data/pretrain/embed_corpus_train
  valid_path: data/pretrain/embed_corpus_valid