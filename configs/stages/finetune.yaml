train:
  batch_size: 2
  total_steps: 400
  grad_accum_steps: 32
  early_stopping: 50
  log_every: 10

optim:
  lr: 3e-5
  betas: [0.9, 0.95]
  weight_decay: 0.01

scheduler:
  eta_min_ratio: 0.01

data:
  train_path: data/finetune/qa_samples.json
  valid_path: data/finetune/qa_samples_val.json